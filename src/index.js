/**
 * Will handle all of our imports
 * 
 * Makes it easy to import all components in one file
 */

import React from "react";
import ReactDOM from "react-dom";
import App from './App';

ReactDOM.render(<App />,document.getElementById("root"));
